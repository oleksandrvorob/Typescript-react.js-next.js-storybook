import styled from 'styled-components'
import Box from '.'

const Wrapper = styled.div`
  padding: 5%;
`

export default { title: 'File Drop' }

export const Example = () => (
  <Wrapper>
    <Box />
  </Wrapper>
)
